name: multi_step
params:
# 50エポック目と75エポック目に学習率を変更する(値は整数である必要がある)
  milestones:
    - 0.5
    - 0.75
    - 1.00
    - 1.25
  # milestonesで指定したエポックに到達するたびに、
  # 現在の学習率をこの値の倍率(0.1倍)で更新する。
  gamma: 0.1